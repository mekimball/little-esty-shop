<h1>Discounts!</h1>

<% @discounts.each do |discount| %>
<div id="id-<%= discount.id %>">
  <p>Discount: <%= (discount.discount * 100).round(2) %>%, Item Threshold: <%= discount.number_of_items_necessary %> <%= link_to 'Discount Details', merchant_bulk_discount_path(@merchant, discount) %>
  <%= link_to 'Delete Discount', merchant_bulk_discount_path(@merchant, discount), method: :delete %></p>
  </div>
<% end %>

<%= link_to  "New Discount", new_merchant_bulk_discount_path(@merchant) %>



